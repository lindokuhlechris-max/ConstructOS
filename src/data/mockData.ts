import { Activity, DailyReport, Project } from '../types';

export const mockProjects: Project[] = [];
export const mockActivities: Activity[] = [];
export const mockDailyReports: DailyReport[] = [];
